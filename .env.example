# MongoDB Connection String (from MongoDB Atlas)
# Format: mongodb+srv://<username>:<password>@<cluster>.mongodb.net/?appName=<AppName>
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.mpmy7ir.mongodb.net/?appName=Cluster0

# Session secret (use a long random string in production)
SESSION_SECRET=change-me-to-a-long-random-secret

# Optional: SMTP settings for contact form emails
SMTP_HOST=
SMTP_USER=
SMTP_PASS=
EMAIL_FROM=

# Server port (defaults to 5000)
PORT=5000
